index.js is the start point and root file.

All the API routes for Users and Groups are kept in separate .js files and appropriate comments are added.

used jwt tokens for securely transmitting information
